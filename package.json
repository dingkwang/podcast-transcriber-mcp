{
  "name": "podcast-transcriber",
  "version": "0.1.0",
  "description": "A MCP server to fetch podcast RSS feeds, list episodes, and transcribe them using OpenAI API",
  "private": true,
  "type": "module",
  "bin": {
    "podcast-transcriber": "./build/index.js"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "build": "echo 'Skipping build for now'",
    "prepare": "echo 'Skipping prepare step'",
    "watch": "tsc --watch",
    "inspector": "npx @modelcontextprotocol/inspector build/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "0.6.0",
    "openai": "^4.89.0",
    "rss-parser": "^3.13.0",
    "axios": "^1.6.7",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "typescript": "^5.3.3"
  }
}
